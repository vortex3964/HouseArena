# Optional dev-only prefill for the in-app backend form (Register/Login).
# The app does NOT require .env — the owner pastes these inside the app and
# they are saved in SecureStore. Fill these locally so you don't retype
# while coding; leave empty in git.
EXPO_PUBLIC_SUPABASE_URL=""
EXPO_PUBLIC_SUPABASE_KEY=""
